import React from "react";

const Header = () => {
  return (
    <header style={{ marginBottom: 10 }}>
      <h1>My App</h1>
      <button>Chande the dark mode</button>
    </header>
  );
};

export default Header;
